/*
 * void is a good practice.
 * The printf() function sends a formatted string
 * to the standard output (the display)
 */

#include <stdio.h>

/**
 * main - prints a given string.
 *
 * Return: (0).
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
